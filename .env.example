# MODELO — copie para .env e preencha. Este arquivo (.env.example) é público:
# NUNCA cole tokens reais aqui. Os segredos vão no .env (ignorado pelo git)
# e, em produção, nas Environment Variables da Vercel.

# Turso (libSQL) — crie em https://turso.tech
# Dev local pode usar um arquivo SQLite, sem precisar do Turso:
#   TURSO_DATABASE_URL=file:local.db
TURSO_DATABASE_URL=file:local.db
TURSO_AUTH_TOKEN=

# Senha da tela de login de demonstração (seção 11.4 do relatório)
APP_PASSWORD=luminatech2026
